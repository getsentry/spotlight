<style>
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes fade-in-neon {
    from {
      color: #ffffff00;
      text-shadow:
        0 0 5px #ffffff00;
    }
    to {
      color: #ffffff;
      text-shadow:
        0 0 10px #ffffff,
        0 0 50px #00ffff,
        0 0 75px #00ffff;
    }
  }

  .wrapper {
    height: calc(100vh - 5.5rem);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    flex-direction: column;
  }

  .neon-header {
    color: #fff0;
    letter-spacing: 12px;
    text-shadow:
      0 0 5px #fff0;
    font-weight: 500;
    font-size: 3rem;
    text-align: center;
    margin-bottom: 1rem;
    animation: fade-in 1s ease-in-out 0s forwards, fade-in-neon 1s ease-out 0.5s forwards;
    font-family: "Raleway", sans-serif;
  }

  .sub-heading {
    color: #fff;
    font-size: 1.5rem;
    font-weight: 500;
    text-align: center;
    margin-bottom: 3rem;
    font-weight: 200;
    opacity: 0;
    animation: fade-in 1s ease-in-out 1s forwards;
  }

  .actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    opacity: 0;
    animation: fade-in 1s ease-in-out 1s forwards;

    & > a {
      display: block;
      height: 3rem;
      line-height: 3rem;
      padding: 0 1.5rem;
      cursor: pointer;
      font-weight: 600;
      border: 1px solid #ffffff80;
      opacity: 0.5;
      box-shadow: 0 0 5px #fff8, 0 0 5px #fff8 inset;
      text-decoration: none;
      color: #fff;
      transition: all 0.1s ease-in-out;
      border-radius: 0.5rem;

      &:hover {
        opacity: 1;
      }
      &:first-child {
        border: 1px solid rgba(113, 255, 255, 0.5);
        color: rgba(113, 255, 255, 1);
        box-shadow: 0 0 5px rgba(113, 255, 255, 0.5), 0 0 5px rgba(113, 255, 255, 0.5) inset;
      }
    }
  }

  @media (min-width: 768px) {
    .neon-header {
      font-size: 5rem;
      letter-spacing: 24px;
    }
    .sub-heading {
      letter-spacing: 3px;
    }
  }
</style>

<div class="wrapper">
  <h1 class="neon-header">Spotlight</h1>
  <div class="sub-heading">The debug toolbar you always wanted</div>
  <div class="actions">
    <a href="/what-is-spotlight/">Get started</a>
    <a href="/reference/configuration/">Read the docs</a>
  </div>
</div>