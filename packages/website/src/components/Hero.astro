---
import { Fragment } from 'react';
import ButtonLink from './ButtonLink.astro';

// Used to override the default hero component
---

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes fade-in-neon {
    from {
      color: #ffffff00;
      text-shadow: 0 0 5px #ffffff00;
    }
    to {
      color: #ffffff;
      text-shadow:
        0 0 10px #ffffff,
        0 0 50px #a5b4fc,
        0 0 75px #a5b4fc;
    }
  }

  .wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    flex-direction: column;
    padding-top: 4rem;
    padding-bottom: 8rem;
    min-height: calc(100vh - 54px);
  }

  .neon-header {
    color: #fff0;
    letter-spacing: 6px;
    text-shadow: 0 0 5px #fff0;
    font-weight: 500;
    font-size: 3rem;
    text-align: center;
    margin-bottom: 1rem;
    line-height: 1.5;
    animation:
      fade-in 1s ease-in-out 0s forwards,
      fade-in-neon 1s ease-out 0.25s forwards;
    font-family: 'Raleway', sans-serif;
  }

  .sub-heading {
    color: #fff7;
    font-size: 1.2rem;
    font-weight: 400;
    text-align: center;
    max-width: 80%;
    opacity: 0;
    animation: fade-in 1s ease-in-out 0.5s forwards;
  }

  .cta {
    opacity: 0;
    animation: fade-in 1s ease-in-out 0.75s forwards;
  }

  .hero-image {
    width: 100%;
    margin-bottom: 2rem;
    border-radius: 0.5rem;
    opacity: 0;
    animation: fade-in 1s ease-in-out 1s forwards;
  }

  @media (min-width: 768px) {
    .sub-heading {
      letter-spacing: 3px;
    }
  }
</style>

<div class="wrapper">
  <h1 class="neon-header">Debuggability Elevated</h1>
  <div class="sub-heading mb-4">Spotlight is a debugging tool that can be embedded in your web app</div>
  <div class="cta flex items-center justify-center gap-2 py-4 mb-4">
    <button class="px-3 py-3 text-base text-s text-white bg-gray-800 border border-transparent rounded-md hover:bg-gray-700 cursor-pointer"
    onclick="Spotlight.openSpotlight()">Checkout Live</button>
    or
    <ButtonLink href="/about/">Learn More</ButtonLink>
  </div>
</div>
