---
// Used to override the default hero component
---

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes fade-in-neon {
    from {
      color: #ffffff00;
      text-shadow:
        0 0 5px #ffffff00;
    }
    to {
      color: #ffffff;
      text-shadow:
        0 0 10px #ffffff,
        0 0 50px #a5b4fc,
        0 0 75px #a5b4fc;
    }
  }

  .wrapper {
    height: calc(100vh - 5.5rem);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    flex-direction: column;
  }

  .neon-header {
    color: #fff0;
    letter-spacing: 12px;
    text-shadow:
      0 0 5px #fff0;
    font-weight: 500;
    font-size: 3rem;
    text-align: center;
    margin-bottom: 0rem;
    line-height: 1.5;
    animation: fade-in 1s ease-in-out 0s forwards, fade-in-neon 1s ease-out 0.5s forwards;
    font-family: "Raleway", sans-serif;
  }

  .sub-heading {
    color: #fff;
    font-size: 1.5rem;
    font-weight: 500;
    text-align: center;
    font-weight: 200;
    opacity: 0;
    animation: fade-in 1s ease-in-out 1s forwards;
  }

  .actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    opacity: 0;
    animation: fade-in 1s ease-in-out 1s forwards;
  }

  .actions > a {
    display: block;
    height: 3rem;
    line-height: 3rem;
    padding: 0 1.5rem;
    cursor: pointer;
    font-weight: 600;
    border: 1px solid #ffffff70;
    opacity: 0.7;
    box-shadow: 0 0 5px #fff8, 0 0 5px #fff8 inset;
    text-decoration: none;
    color: #ffffffcc;
    transition: all 0.1s ease-in-out;
    border-radius: 0.5rem;
  }

  .actions > a:hover {
    opacity: 1;
  }
  .actions > a:first-child {
    border: 1px solid #a5b4fcee;
    color: #a5b4fc;
    box-shadow: 0 0 5px #a5b4fcee, 0 0 5px #a5b4fcee inset;
  }

  @media (min-width: 768px) {
    .neon-header {
      font-size: 5rem;
      letter-spacing: 24px;
    }
    .sub-heading {
      letter-spacing: 3px;
    }
  }

  .hero-image {
    width: 100%; 
    margin-top: 2rem; 
    margin-bottom: 2rem; 
    border-radius: 0.5rem;
    opacity: 0; animation: fade-in 1s ease-in-out 1s forwards;
  }
</style>

<div class="wrapper">
  <h1 class="neon-header">Spotlight</h1>
  <div class="sub-heading">The debug toolbar you always wanted</div>
  <img src="/images/hero-showcase.png" alt="Spotlight Hero" class="hero-image" />
  <div class="actions">
    <a href="/what-is-spotlight/">Read the docs</a>
  </div>
  
</div>