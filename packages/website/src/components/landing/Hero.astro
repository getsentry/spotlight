---
---

<div class="px-6 py-16 max-w-7xl mx-auto" style="border-bottom: 1px solid #191919; border-left: 1px solid #191919; border-right: 1px solid #191919; background: linear-gradient(to bottom, #191919 0%, #000000 100%);">
  <div class="text-center">
    <!-- Hero Content -->
    <h1 class="text-4xl md:text-6xl max-w-3xl mx-auto font-bold mb-6 text-white">
      Debug locally with the power of Sentry
    </h1>

    <p class="text-xl text-[#a5a5a5] mb-12 max-w-3xl mx-auto leading-relaxed">
      Spotlight brings the power of Sentry into your local development environment.
      Debug faster with real-time error tracking, traces, and logs—right where you code.
    </p>

    <!-- Command Boxes -->
    <div class="space-y-2 mb-12">
      <div class="inline-flex items-center justify-between max-w-xs w-full mx-auto px-2 rounded-lg font-mono text-sm bg-[#161616] border border-[#1d1d1d]">
        <div class="flex items-center gap-3">
          <span class="text-[#a5a5a5]">$</span>
          <span class="select-all text-white">npx -y @spotlightjs/spotlight</span>
        </div>
         <button
           class="p-2 rounded copy-btn bg-transparent hover:bg-white/10 transition-colors border-0 shadow-none outline-none"
           data-copy="npx -y @spotlightjs/spotlight"
           aria-label="Copy command"
         >
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#a5a5a5" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-copy-icon lucide-copy w-4 h-4">
            <rect width="14" height="14" x="8" y="8" rx="2" ry="2"/>
            <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/>
          </svg>
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  document.querySelectorAll('.copy-btn').forEach(btn => {
    btn.addEventListener('click', async () => {
      const text = btn.getAttribute('data-copy');
      if (text) {
        await navigator.clipboard.writeText(text);
        const originalText = btn.textContent;
        btn.textContent = 'Copied!';
        btn.classList.add('bg-green-500/30');
        setTimeout(() => {
          btn.textContent = originalText;
          btn.classList.remove('bg-green-500/30');
        }, 2000);
      }
    });
  });
</script>
